dataset:
  name: imagenet100
  root: ./data/imagenet100
  subset_per_class: 200          # the maximal number of the samples per class in cal set
  full_loader_batch_size: 128    # using for full dataset loader

transform:
  input_size: 224